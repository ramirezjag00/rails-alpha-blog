<!DOCTYPE html>
<html>
  <head>
    <title>AlphaBlog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render 'layouts/navigation' %>
    <!-- by default, render method looks at the articles routes not which dir you are currently in -->
    <%= render 'layouts/messages' %>
    <div class="container">
      <!-- yield is basically the instance of all contents of all views which renders the page depending on with path you are in -->
      <!-- we want the contents to be inside a container -->
      <%= yield %>
    </div>
  </body>
</html>
